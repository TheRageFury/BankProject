package requests.specifiers;

/**
 * This enumerator represents the possible
 * parameters for a transaction request.
 */
public enum TransactionRequestParameters {
    SINGLE_TYPE,
    RANGE_DATES,
    GROUP_WORDS,
    GROUP_TAGS,
}
